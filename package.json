{
  "name": "booking-com-playwright-tests",
  "version": "1.0.0",
  "description": "Playwright TypeScript automation tests for booking.com (search + filters)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:search": "playwright test tests/search.spec.ts",
    "test:filters": "playwright test tests/filters.spec.ts",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0"
  }
}
